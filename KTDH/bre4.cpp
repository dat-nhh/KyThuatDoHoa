#include<stdio.h>
#include<graphics.h>
/*
void bre(int xa, int ya, int xb, int yb, int color)
{
	int dx=xb-xa;
	int dy=yb-ya;
	int x=xa;
	int y=ya;
	int Q=2*dy+dx;
	for(x=xa;x>=xb;x--)
	{
		printf("x=%d, y=%d, Q=%d\n",x,y,Q);
		putpixel(x,y,color);
		if(Q<=0)
		{
			y=y+1;
			Q=Q+2*dy;
		}
		else
		{
			y=y;
			Q=Q+2*dx+2*dy;
		}
	}
}

int main()
{
	initwindow(300,300);
	bre(120,30,20,80,2);
	getch();
}
*/
